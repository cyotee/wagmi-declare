{
  "name": "@daosys/wagmi-declare",
  "version": "0.1.0",
  "description": "Lightweight helpers and types to build UIs from contract-list schemas (framework-agnostic)",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "bin": {
    "wagmi-declare": "./bin/generate-contractlist.mjs"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc -p .",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "node ./bin/validate-contractlist.mjs",
    "generate": "node ./bin/generate-contractlist.mjs"
  },
  "author": "indexedex",
  "license": "MIT",
  "peerDependencies": {
    "wagmi": "^1.0.0",
    "viem": "^1.0.0",
    "react": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^18.0.0",
    "vitest": "^1.0.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "jsdom": "^22.0.0",
    "ajv": "^8.12.0"
  }
}
